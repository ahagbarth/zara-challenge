import styled from '@emotion/styled';

export const BagContainer = styled.div`
  display: flex;
  flex-direction: row;
  align-items: center;
  gap: 6px;
  padding: 4px 0;
  font-size: 16px;
  font-weight: 300;
  width: fit-content;
`;
